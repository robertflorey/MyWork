@model List<HRPortal.Models.Category>

@{
    ViewBag.Title = "ManageCategories";
}

<div class="row">
    <div class="col-xs-12">
        <h3>Manage Categories</h3>
        @Html.ActionLink("Add Category", "AddCategory", "HR", null, new { @class = "btn btn-primary" })
    </div>
</div>
<div class="row">
    <div class="col-xs-12">
        <table class="table table-striped" style="width: auto;">
            <thead>
                <tr>
                    <th>Category</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var category in Model)
                {
                    <tr>
                        <td>@category.CategoryName</td>
                        <td>
                            @Html.ActionLink("Delete", "DeleteMajor", "Admin", new { categoryId = category.CategoryId }, null)
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

